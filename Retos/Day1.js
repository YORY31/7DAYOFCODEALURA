

//Desafio resuelto 
let numUno = 1;
let strUno = "1";
let numTrenta = 30;
let strTrenta = "30";
let numDiez = 10;
let strDiez = "10";

if (numUno == strUno) {
  console.log(
    `Las variables ${numUno} y ${strUno} tienen el mismo valor, pero tipos diferentes`
  );
} else {
  console.log(`Las variables ${numUno} y ${strUno} no tienen el mismo valor`);
}

if (numTrenta === strTrenta) {
  console.log(
    `Las variables ${numTrenta} y ${strTrenta} tienen el mismo valor, y el mismo tipo`
  );
} else {
  console.log(
    `Las variables ${numTrenta} y ${strTrenta} no tienen el mismo tipo`
  );
}

if (numDiez == strDiez) {
  console.log(
    `Las variables ${numDiez} y ${strDiez} tienen el mismo valor, pero tipos diferentes`
  );
} else {
  console.log(`Las variables ${numDiez} y ${strDiez} no tienen el mismo valor`);
}
